<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AudiobookApp.Views.PlayerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AudiobookApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Spacing="10">

        <Border Width="180"
        Height="180"
        CornerRadius="8"
        HorizontalAlignment="Center"
        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
        BorderThickness="1">
            <Grid>
                <Image x:ConnectionId='9' x:Name="CoverImage"
               Stretch="UniformToFill"/>
                <FontIcon x:ConnectionId='10' x:Name="CoverPlaceholder"
                  Glyph="&#xE8D6;"
                  FontSize="48"
                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <TextBlock x:ConnectionId='2'
            x:Name="TitleText"
            FontSize="18"
            FontWeight="SemiBold"
            TextAlignment="Center"
            TextWrapping="WrapWholeWords"
            HorizontalAlignment="Center"/>

        <TextBlock x:ConnectionId='3'
            x:Name="AuthorText"
            FontSize="14"
            Opacity="0.7"
            TextAlignment="Center"
            TextWrapping="WrapWholeWords"
            HorizontalAlignment="Center"/>

        <StackPanel
            Orientation="Horizontal"
            HorizontalAlignment="Center"
            Spacing="20">

            <Button x:ConnectionId='5' x:Name="PreviousButton">
                <SymbolIcon Symbol="Previous"/>
            </Button>

            <Button x:ConnectionId='6'
                x:Name="PlayPauseButton"
                Width="64"
                Height="64"
                                             >

                <SymbolIcon x:ConnectionId='8'
                x:Name="PlayPauseIcon"
                Symbol="Play"/>
            </Button>

            <Button x:ConnectionId='7' x:Name="NextButton">
                <SymbolIcon Symbol="Next"/>
            </Button>

        </StackPanel>

        <MediaPlayerElement x:ConnectionId='4'
            x:Name="AudioPlayer"
            AreTransportControlsEnabled="False"
            AutoPlay="False"
            Visibility="Collapsed"/>

    </StackPanel>
</Page>

