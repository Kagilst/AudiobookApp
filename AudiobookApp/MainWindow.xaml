<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="AudiobookApp.MainWindow" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="using:AudiobookApp" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        mc:Ignorable="d" Title="AudiobookApp">

    <NavigationView x:Name="RootNav" PaneDisplayMode="Left" IsBackButtonVisible="Collapsed">
        
        <!-- LEFT PANE -->
        <NavigationView.PaneCustomContent>
            
            <Grid RowDefinitions="Auto,Auto,*,Auto">
                
                <!-- ALL BOOKS (fixed top) -->
                <Button Click="AllBooks_Click"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Left"
                        Padding="12,10"
                        Margin="0"
                        Grid.Row="0">

                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <SymbolIcon Symbol="Library"/>
                        <TextBlock Text="All Books"/>
                    </StackPanel>

                </Button>

                <!-- CATEGORY LIST (ONLY reorderable items) -->
                <ListView x:Name="CategoryList" Grid.Row="1" CanReorderItems="True" AllowDrop="True" SelectionMode="Single">
                    
                    <ListView.ItemTemplate>
                        
                        <DataTemplate>
                            
                            <Grid Padding="6">
                                
                                <Grid.ContextFlyout>
                                    
                                    <MenuFlyout>
                                        
                                        <MenuFlyoutItem Text="Rename" Click="RenameCategory_Click"/>
                                        <MenuFlyoutItem Text="Delete" Click="DeleteCategory_Click"/>
                                        
                                    </MenuFlyout>
                                    
                                </Grid.ContextFlyout>
                                
                                <TextBlock Text="{Binding Name}" />
                                
                            </Grid>
                            
                        </DataTemplate>
                        
                    </ListView.ItemTemplate>
                    
                </ListView>
                <!-- ADD CATEGORY (fixed bottom) -->
                <Button Click="AddCategory_Click"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Left"
                        Padding="12,10"
                        Margin="0"
                        Grid.Row="2">

                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <SymbolIcon Symbol="Add"/>
                        <TextBlock Text="Add Category"/>
                    </StackPanel>

                </Button>

            </Grid>
            
        </NavigationView.PaneCustomContent>
        <!-- MAIN CONTENT -->
        <Frame x:Name="ContentFrame"/>
    </NavigationView>
</Window>